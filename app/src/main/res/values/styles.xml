<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:dialogTheme">@style/AppThemeTitle</item>
    </style>

    <!-- Used to guarantee a title to the dialogs on all platforms -->s
    <style name="AppThemeTitle" parent="Theme.MaterialComponents.DayNight.Dialog.Alert">
        <item name="android:windowNoTitle">false</item>
    </style>

    <!-- Transparent theme for side activities(e.g. triggered by notification intents) -->
    <style name="TransparentTheme" parent="@style/AppTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

</resources>
